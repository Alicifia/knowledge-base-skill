---
name: knowledge-base
description: 把任意本地文件夹变成可检索的知识库系统。当用户想搭建知识库、指定知识库目录、导入/归档文档到知识库、在知识库中检索问答、查找某篇笔记的关联文档、管理多个知识库或跨知识库搜索时触发。支持向量检索、SQLite 持久化存储、多文档关联（wikilink + 相似度）、多知识库注册表与跨库统一检索，完全离线本地运行。
---

# 本地知识库

> Copyright (c) 2026 Alicifia · MIT License · https://github.com/Alicifia/knowledge-base-skill

把用户指定的本地目录变成真正的知识库：文档切块 → 向量化（CJK 单字+二元组 TF-IDF）→ SQLite 持久化 → 余弦相似度检索 + 文档关联。**零第三方依赖、零网络、零配置**，任何 Python 3.8+ 都能跑。

支持**多知识库**：所有库登记在全局注册表（`~/.knowledge-base/config.json`）里，可以按名称切换、一览全部库、或一次提问跨全部库统一检索。每个库的索引自包含在自己的目录里，互不影响，换机直接拷贝文件夹即可。

## 核心脚本

`scripts/kb.py`（相对于本 SKILL.md 所在目录）。一律用 `python scripts/kb.py <命令>` 调用（Windows 上 `python` 与 `python3` 均可，哪个能用用哪个）。

| 命令 | 作用 |
|---|---|
| `init --root <目录> [--name 别名]` | 指定本地知识库目录并初始化（不存在会自动创建），自动登记进注册表并设为当前激活库；别名默认用目录名 |
| `use --root <目录或名称>` | 在多个知识库之间切换，可直接用注册表里的名称 |
| `libraries` | 列出注册表里的全部知识库：名称、目录、文档/块数、是否当前激活、目录是否还在 |
| `forget <目录或名称>` | 从注册表注销某个知识库。**只删登记信息，不删目录里的任何文件**，随时可 init 登记回来 |
| `add <文件或目录> [...] [--strict]` | 导入文档并建索引；目录会递归扫描；重复 add 自动跳过未变化文件。`--strict`：任何文件读取失败立即退出（exit 2） |
| `remove <路径或标题> [...]` | 从库中移除文档 |
| `search "<问题或关键词>" [--top 8] [--per-doc] [--all]` | 向量检索，返回 JSON：相关块、所在文档、标题层级、相似度分数、摘要。`--all`：跨注册表内全部知识库统一检索 |
| `related <路径或标题> [--top 5]` | 查某篇文档的关联文档：显式 `[[wikilink]]`/markdown 链接（双向）+ 向量相似文档 |
| `list` | 列出库内全部文档及块数 |
| `status` | 当前激活库、已登记库清单、文档/块/词表/链接统计 |
| `reindex` | 全量重建索引 |

## 支持格式

- 原生：`.md` / `.markdown` / `.txt`
- 电子书：`.epub`、`.mobi`、`.azw3`、`.azw`（HuffCDIC 压缩的老 Kindle 文件暂不支持）
- 可选：`.docx`（需 python-docx）、`.pdf`（需 pdfplumber）；缺库时明确提示并跳过

## 使用流程

1. **首次使用**：用户给出目录 → `python scripts/kb.py init --root "<目录>"`。
2. **导入**：`python scripts/kb.py add "<目录或文件>"`。新文件、改动过的文件自动更新索引。
3. **检索问答**：用户提问 → `search "<用户问题>"` → 从 hit 里取 snippet 作答，**答案注明出处文件**。score < 0.05 的结果基本无关，忽略。
4. **关联探索**：用户问"这篇笔记和哪些相关" → `related <标题>`。
5. **多库**：`libraries` 一览全部库，`use` 按名称切换。
6. **跨库检索**：`search "<问题>" --all`，结果标注来源库。

## 行为约定

- 用户说"建个知识库"但没给目录 → 问一句想放哪，别自己造目录。
- 登记了多个库且用户没指定检索范围时，优先 `search --all`，并注明每条结果来自哪个库。
- `search` 返回结构化 JSON，直接解析；用人话总结 + 标注来源文件，不要贴原始 JSON。
- 检索结果为空或分数都很低时，如实说"库里没有相关内容"，不要编造。
- `remove` 只删索引不删原文件；`forget` 只删注册表登记不删任何文件。
- 所有数据都在 `<知识库目录>/.knowledge-base/index.sqlite`，换机迁移直接拷贝整个知识库目录。

---
Copyright (c) 2026 Alicifia. Released under the MIT License.